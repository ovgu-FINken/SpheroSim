<launch>

  <arg name="x" default="1" />
  <arg name="y" default="1" />
  <arg name="z" default="0.04" />

  <!-- URDF and TF support -->
  <param name="robot_description" command="$(find xacro)/xacro $(find sphero_description)/robots/sphero.gazebo.xacro" />
  <node name="robot_state_publisher" pkg="robot_state_publisher" type="robot_state_publisher" >
    <param name="publish_frequency" value="50.0"/>
  </node>

  <!-- Put a robot in gazebo, make it look pretty -->
  <node name="urdf_spawner" pkg="gazebo_ros" type="spawn_model" respawn="false" output="screen"
        args="-urdf -x $(arg x) -y $(arg y) -z $(arg z) -model sphero -param robot_description"/>
<!-- -file $(find sphero_description)/robots/sphero.urdf -->

</launch>
